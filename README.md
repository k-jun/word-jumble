# Word Jumble

## run

以下のコマンドでサーバーを起動します。

```bash
deno run -A src/main.ts
```

以下のコマンドで `./words.txt` を最新の状態へ更新します。

```bash
deno run -A batch/main.ts
```

## test

```bash
deno -A test
```

<!-- winter-aconite -->
